#include <bits/stdc++.h>
using namespace std;
int main()
{
	string a;
	string b[1000];
	int top=0;
	cin>>a;
	for (int i=0;i<a.length();i++)
	{
		if (a[i]=='(')
		{
			b[top]='(';
			top++;
		}
		else if (a[i]==')')
		{
			b[top]
		}
	}
	return 0;
}

